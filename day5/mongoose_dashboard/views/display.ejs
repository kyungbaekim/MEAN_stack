<html>
<head>
  <title>Mongoose Dashboard - Edit</title>
  <style>
    .wrapper{
      width: 500px;
      margin: 0px auto;
      text-align: center;
    }
    table{
      margin: 0px auto;
    }
    .send{
      margin-top: 10px;
      margin-left: 130px;
    }
    .error{
      color: red;
    }
  </style>
</head>
<body>
  <div class='wrapper'>
    <div class='title'>
      <h1>Edit animal in your database</h1>
    </div>
    <div class='form'>
      <form action='/animals/edit/<%= animal._id %>' method='post'>
        <table>
          <tr>
            <td>Name:</td>
            <td><%= animal.name %></td>
          </tr>
          <tr>
            <td>Description:</td>
            <td><%= animal.description %></td>
          </tr>
          <tr>
            <td colspan='2' align=center>
              <input class='send' type='submit' value='Edit animal!'>
            </td>
          </tr>
        </table>
        <% if(typeof(errors) != 'undefined' && errors != null){ %>
          <br><table>
          <% for(x in errors){ %>
            <tr><td class='error'><%= errors[x].message %></td></tr>
          <% } %>
          </table>
        <% } %>
      </form>
    </div>
  </div>
</body>
</html>
